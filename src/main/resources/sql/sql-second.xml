<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.second.group.mapper.SecondMapper">
	<select id="selectSecondList" resultType="com.second.group.dto.RecipeDto">
		<![CDATA[
			SELECT 
				idx, title,
			DATE_FORMAT
				(created_datetime, '%Y.%m.%d') 
			AS 
				created_datetime
			FROM 
				recipe
			WHERE
			    idx
		]]>
	</select>
	<select id="selectCommentList" resultType="com.second.group.dto.CommentDto">
		<![CDATA[ 
		SELECT 
			cmtidx, cmt_content, 
		DATE_FORMAT
			(cmt_datetime, '%Y.%m.%d') 
		AS 
			cmt_datetime
		FROM 
			comment
		WHERE
			cmtidx
		
		]]>
	
	</select>
</mapper>